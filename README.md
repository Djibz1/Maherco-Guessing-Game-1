# TOP 10 Multiplayer — Professional

## Run locally
```bash
npm install
npm start
```
Open http://localhost:3000

Every browser tab is a fresh Socket.IO client. No localStorage/sessionStorage or URL-based room restoration is used.
